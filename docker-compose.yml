services:
  backend:
    build: ./python_fastapi
    ports: 
      - "8000:8000"
    env_file: 
      - ./python_fastapi/.env

  frontend:
    build: ./job_tracker_frontend
    ports: 
      - "5173:80"